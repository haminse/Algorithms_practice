def floodFill(image, sr: int, sc: int, color: int):

    # select the 4 direction adjecent & != 0 value pixel 
    # change the pixel value with color vlaue
    # check the adjecent & != 0  pixel from new pixels
    # 

    return image


a =floodFill(image = [[1,1,1],[1,1,0],[1,0,1]], sr = 1, sc = 1, color = 2)
print(a)